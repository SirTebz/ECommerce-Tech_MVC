@model SirTebz_Tech.Models.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Login";
    Layout = "_Layout";
}

<div class="auth-page d-flex align-items-center py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="auth-card">
                    <div class="auth-header text-center mb-4">
                        <div class="auth-logo mb-2">
                            <span class="logo-sir">Sir</span><span class="logo-tebz"> Tebz</span><span class="logo-dot">.</span><span class="logo-tech">Tech</span>
                        </div>
                        <h2 class="auth-title">Welcome back</h2>
                        <p class="auth-subtitle">Sign in to your account</p>
                    </div>

                    <form asp-action="Login" method="post">
                        <input type="hidden" asp-for="ReturnUrl" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 mb-3 small"></div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label auth-label">Email address</label>
                            <div class="input-group">
                                <span class="input-group-text auth-icon"><i class="bi bi-envelope"></i></span>
                                <input asp-for="Email" class="form-control auth-input" placeholder="you@example.com" />
                            </div>
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Password" class="form-label auth-label">Password</label>
                            <div class="input-group">
                                <span class="input-group-text auth-icon"><i class="bi bi-lock"></i></span>
                                <input asp-for="Password" class="form-control auth-input" placeholder="••••••••" />
                                <button class="btn auth-icon" type="button" onclick="togglePwd(this)">
                                    <i class="bi bi-eye"></i>
                                </button>
                            </div>
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div class="form-check">
                                <input asp-for="RememberMe" class="form-check-input" />
                                <label asp-for="RememberMe" class="form-check-label auth-label">Remember me</label>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-auth w-100 mb-3">
                            <i class="bi bi-box-arrow-in-right me-2"></i>Sign In
                        </button>

                        <div class="auth-divider"><span>Don't have an account?</span></div>
                        <a asp-action="Register" class="btn btn-auth-outline w-100 mt-3">
                            <i class="bi bi-person-plus me-2"></i>Create Account
                        </a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}